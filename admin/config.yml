backend:
  name: git-gateway
  branch: main

publish_mode: editorial_workflow

media_folder: "images"
public_folder: "images"

collections:
  - name: "productos"
    label: "Catalogo antiguo"
    label_singular: "Gorra (antigua)"
    delete: true
    files:
      - name: "catalogo"
        label: "Catalogo de gorras (viejo)"
        file: "data/productos.json"
        fields:
          - label: "Gorras"
            name: "productos"
            widget: "list"
            summary: "{{fields.numero}} {{fields.nombre}} - {{fields.categoria}} - {{fields.precio}} - {{fields.descripcion}}"
            fields:
              - {label: "Numero (N)", name: "numero", widget: "string", required: false}
              - {label: "Nombre", name: "nombre", widget: "string"}
              - label: "Categoria"
                name: "categoria"
                widget: "relation"
                collection: "categorias"
                search_fields: ["nombre"]
                value_field: "nombre"
                display_fields: ["nombre"]
              - {label: "Descripcion", name: "descripcion", widget: "text", required: false}
              - {label: "Precio", name: "precio", widget: "string"}
              - label: "Fotos"
                name: "fotos"
                widget: "list"
                field: {label: "Foto", name: "foto", widget: "image"}

  - name: "gorras"
    label: "Gorras"
    label_singular: "Gorra"
    folder: "data/gorras"
    create: true
    delete: true
    slug: "{{slug}}"
    summary: "{{nombre}} - {{categoria}} - {{precio}}"
    fields:
      - {label: "Numero (N)", name: "numero", widget: "string", required: false}
      - {label: "Nombre", name: "nombre", widget: "string"}
      - label: "Categoria"
        name: "categoria"
        widget: "select"
        options: ["Airbag", "Fuxion", "Nino", "Otras"]
      - {label: "Descripcion", name: "descripcion", widget: "text", required: false}
      - {label: "Precio", name: "precio", widget: "string"}
      - label: "Foto principal (para vista previa en el panel)"
        name: "foto_principal"
        widget: "image"
        required: false
      - label: "Fotos"
        name: "fotos"
        widget: "list"
        field: {label: "Foto", name: "foto", widget: "image"}

  - name: "categorias"
    label: "Categorias"
    label_singular: "Categoria"
    folder: "data/categorias"
    extension: "json"
    format: "json"
    create: true
    delete: true
    slug: "{{slug}}"
    fields:
      - {label: "Nombre", name: "nombre", widget: "string"}
